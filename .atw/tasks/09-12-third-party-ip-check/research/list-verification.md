# A「轻量清单」实现与验证

## 范围与基线

- 用户已选择 A，见 `prototype-directions.md`；本轮用户再次运行 `/atw-implement`，并确认审查基线 `e759dd8f71b5966af7acd6a02d5704a0ef7b074f`，当前分支 `main`。
- 生产修改只有共享 `ThirdPartyIpCheck()` 与英文词典的三个新增键：类别、用途、查看提示。沿用已有“网站”译文；九站目录数据和三个页面接入文件不变。
- 外层保留 `ToolCard`；内部变为细分隔线清单，`md` 起四列，移动端堆叠。展开提示全宽，入口保持本行原位；无卡片嵌套、固定行高、JS 测高或状态管理。
- 继续使用原生 `details/summary`，短提示入口以带站名的 `aria-label` 区分；链接、全部用途和风险限定不变。未合入原型的其他变体、切换器或开发配置。
- PRD 已按用户选择修订展示要求、AC11 和测试决策；旧卡片记录保留为历史，不与当前要求混用。

## 布局红绿证据

预先在旧版生产构建运行以下真实 DOM 检查，随后修改布局、重新构建并运行同一脚本：

```bash
pnpm build
# 已有本地 preview 服务：127.0.0.1:4173
node .atw/tasks/09-12-third-party-ip-check/research/check-list-layout.js
```

脚本复用本机 `playwright-cli` / Chrome，不安装框架。外部请求在浏览器边界拦截，查询使用文档地址 `203.0.113.10` 合成响应。

两页 × 中英文 × 深浅主题 × 1440/1024/768/390/320px × 全收起/首项展开/全部展开，共 **120 个组合**。

| 指标                               | 修改前         | 修改后         |
| ---------------------------------- | -------------- | -------------- |
| 失败断言                           | 2384           | 0              |
| 四列表头与对应列边缘误差           | 不满足清单要求 | 全部 ≤1 CSS px |
| 全宽提示、入口原位、列互不覆盖     | 不满足清单要求 | 全部通过       |
| 九站逐行、移动堆叠、文案无横向溢出 | 按旧版卡片渲染 | 全部通过       |

布局批量开合通过原生 `details.open` 构造，不将它描述为鼠标测试。独立交互另见下一节。

本机结果文件：`/tmp/ip-list-layout-red.json`、`/tmp/ip-list-layout-result.json`。脚本可重复运行，失败时退出码 1。

## 交互与查询回归

完整重跑既有 `/tmp/ip-directory-browser-check.js`，结果为 `/tmp/ip-list-browser-result.json`：

- 八种状态：未查询、提交后无数据加载中、成功、缓存刷新中、缓存刷新失败、无缓存失败、首页检测失败、持久缓存查询结果。目录始终唯一且位置正确。
- 两页均验证首次挂载收起、鼠标和 Enter 同时打开两站、Space 独立关闭、Tab/Shift+Tab 独立聚焦、可见 outline。
- 九站真实 DOM 的 href、名称、target、rel 均符合固定导航契约，链接不嵌套于 summary。
- 两页各用键盘及鼠标激活外链，共四次主动导航；新标签 `window.opener === null`，原页面展开状态不变。
- 16 个语言/主题/宽度/页面组合全部展开，无中文回退、内容漂移或溢出；末尾提示能滚动到不被固定导航遮挡的位置。
- 九站自动请求 **0**；页面运行时异常 **0**。主动导航单独计数，所有远端请求均拦截，不依赖外站在线。

## 自动检查

- `pnpm exec tsc -b`：通过。
- 定向 Node 测试（目录 + i18n）：**7/7** 通过；新表头/短入口的字面量翻译由既有 i18n 扫描覆盖。
- `pnpm build`：通过，含类型检查。
- `pnpm test`：**98/98** 通过，含 Worker 本地 dry-run，未部署。
- `pnpm lint`：退出码 0，原有 **13** 条警告，改动文件无警告。
- 主动 LSP 检查组件、词典及本轮浏览器复查脚本：3 文件 clean，0 diagnostics。

## 截图与验证边界

已实际查看桌面中文浅色首项展开、移动英文深色全部展开截图，核对清单、细线、文案及提示层级。截图：

- `/tmp/ip-list-1440-zh-CN-closed.png`
- `/tmp/ip-list-1440-zh-CN-first-open.png`
- `/tmp/ip-list-1440-zh-CN-all-open.png`
- `/tmp/ip-list-390-en-all-open.png`

移动长截图会把固定导航拍在中间，不等于无法阅读；回归检查已验证提示可滚动到无遮挡区域。截图只含合成测试数据。

仅验证本机 Chrome 和本地生产构建，未验证 Safari/Firefox、全部辅助技术或外站服务能力，未生产部署。技术检查不代替用户最终视觉接受；双轴审查另见 `list-review.md`。
