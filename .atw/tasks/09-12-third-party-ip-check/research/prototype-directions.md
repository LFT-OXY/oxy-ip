# 五套 UI 原型：用户已选 A

用户认为对齐后的目录仍与原站风格不协调，要求五套不同原型，并在比较后明确回复“我选A”。已确认以 **A · 轻量清单** 作为正式实现的视觉方向；目前尚未改动生产目录代码。

- 一次性原型分支：`prototype/ip-directory-five`，原始设计提交：`0ecc345`。
- 独立工作目录：`/tmp/oxy-ip-directory-prototype`。
- 运行：在该工作目录执行 `pnpm prototype`。
- 预览：`http://localhost:5175/?variant=A&lang=zh-CN`，参数支持 A/B/C/D/E。
- 原有 `http://localhost:4173/` 不受影响。

| 变体 | 方向       | 取舍                             |
| ---- | ---------- | -------------------------------- |
| A    | 轻量清单   | 检测结果式的数据行，九站常显     |
| B    | 分组书架   | 四类并列，无嵌套小卡片，九站常显 |
| C    | 分类工作台 | 侧栏选类别，只显示当前类别       |
| D    | 工具切换带 | 选择网站后展示完整说明           |
| E    | 折叠工具箱 | 默认轻入口，展开后查看完整目录   |

页面底部提供五方案直达按钮、循环前后箭头及键盘左右切换；输入框内不拦截方向键。网址保留当前变体，局部类别/站点选择不持久化。原型代码只在开发模式加载。

原型阶段已进行桌面/移动浏览器查看及可运行性编译，未写测试或运行完整测试套件。C/D/E 有意探索不同的信息可见性，不宣称满足当前 PRD 的全部常显/展开要求，现均不作为正式实现目标。

## 已确认的 A 方案

- 首页和 IP 查询页共用紧凑清单：桌面按“类别｜网站｜用途｜查看提示”排列，九站常显，以细分隔线组织，不再嵌套网站小卡片。
- 每站提示在对应行下方占满宽度展开，可独立开合并同时查看多条；网站导航与提示操作分离。
- 移动端自然堆叠，用途和完整提示不截断；保留中英文、九站固定目标、新标签页安全属性及不自动访问第三方等原有边界。
- 本次选择替代上一轮“桌面三/两列卡片与单站横排”的视觉方向。正式实现前应同步修订 PRD 的展示要求、AC11 和相关实现决策，不能拿旧卡片验收标准审查新清单。
- 原型只作为视觉与交互参考，正式实现须重写必要部分；不合入其他四套方案、浮动切换器、开发端口或原型配置。

详细设计说明及全部原型源码保存在 `prototype/ip-directory-five` 的 `0ecc345`，其中 `src/views/components/third-party-ip-prototype-a.tsx` 是所选方案的原始证据。用户确认的是设计方向，不等于正式代码已经通过验收。

任务已退回 `implement`；等待用户运行 `/atw-implement` 启动 A 方案正式实现、验证、审查及提交。
