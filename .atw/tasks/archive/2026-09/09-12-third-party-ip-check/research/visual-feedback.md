# 验收反馈：卡片对齐

## 用户结论

用户在 `http://localhost:4173/` 检查后指出目录小卡片高度参差、视觉不整齐，未接受当前布局。此前的功能与交互测试通过，不代表视觉验收通过。

用户已选择并确认“保留卡片做对齐”，不改成紧凑列表：

- 综合查询、滥用与信誉：桌面三列，同排卡片等高。
- 归属与用途：桌面两列，利用整行宽度，不留空的第三列。
- 流量趋势：单站占满整行，桌面采用站名、用途、提示入口的横向布局。
- 同排站名与初始收起的提示入口对齐；用途可换行，不通过截断或固定高度隐藏文案。
- 移动端自然堆叠，独立展开后的提示完整可读。
- 九站名称、网址、中英文内容、独立链接/提示交互、页面位置及无自动外访约束不变。

## 源码证据与最小修改边界

`src/views/components/third-party-ip-check.tsx:16` 的 `items-start` 使同排项目按内容取不同高度；用途行数变化还造成 summary 基线错位。固定三列也使两项及单项分组产生空洞。

后续只调整共享目录布局及必要验证，不改查询、缓存、站点数据或翻译内容；不要引入 JS 测高或通用布局框架。

## 再次验收

- 两页、中英文、深浅主题、移动/桌面继续检查。
- 在实际 DOM 中测量收起状态的同排卡片高度、标题和提示入口纵坐标，不能只以“不溢出”判定通过。
- 检查三列、两列、单项横向布局及展开/收起后的可读性；给出桌面与移动截图供用户复查。
- 复跑内容/i18n 测试、类型检查、构建及完整测试，重新执行双轴审查。
- 将用户确认的布局要求同步到 PRD，再按更新后的规格审查。

任务已从 `accept` 退回 `implement`，用户随后再次运行 `/atw-implement` 启动修正链。布局要求已同步至 PRD 的 AC11；新的实测结果见 `visual-verification.md`。任务不能沿用上一版的视觉验收结论直接归档。
