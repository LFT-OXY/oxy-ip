# 第07票双轴审查（2026-09-16）

基线：`12e6504676db0f16920ddebd7d5bcd20c91c8ca9`。审查覆盖工作区与未跟踪文件；两轴独立只读审查，修复后以完整补丁再次审查。前两轮审查时票据保持doing；以下保留历史报告。用户授权后三项例外已落实，最终审查报告见文末。功能提交`c2cc8a4`后构建和166/166测试再次通过，票据已done；未推送、未部署、未启动其他票。

## Standards

首轮硬违规：NekoBox人工快照的本轮失败说明会随解析器继承至未来自动成功快照。已改为长期适用的平台说明，具体失败留在研究证据，并补失败后成功回归。

首轮判断项：资产 selected-or-excluded 不能证明排除资产未选入；已补集合互斥、完整集合与等量错换负例。HomeProxy软件源说法原未完成核验，现已补正式软件源索引与Makefile证据。

复审：除已知未完成项外，无新增硬违规。保留1项判断建议：资产集合目前按应用合并检查，可进一步逐平台冻结文件名和架构映射以发现跨平台等量互换；当前规则未发现实际串配。既有批次改为冻结子集不属于放宽覆盖。

## Spec

首轮发现daed缺OpenWrt平台；通过ImmortalWrt正式24.10.4软件源及官方打包源码核实后已补。dae、daed、HomeProxy采用各自软件源版本，不复制Linux新版本；人工快照不填写自动成功时间或未知发布日期。新平台、下载及软件源切换的浏览器12组合证据已复核。

复审剩余3类硬阻塞：

1. Clash .NET、lvory尚未收录：官方身份资料、可用获取入口和图标未能核实。需补可信证据，或用户明确授权历史保留方式（名称身份来自冻结基线，当前无法获取、文字占位、其他未核实字段留空）。
2. ShellCrash、PassWall、PassWall2、Nikki、Nikki RS、Momo、HomeProxy缺官方图标。须取得官方素材，或用户明确允许各项文字占位；旧票例外不扩用。
3. Hey官方明确仅分发源码，当前源码获取方式须用户明确授权；Clash MIX例外不扩用。

未发现其他可证实的必须先修项。以上阻塞未被豁免，不得标整票通过。

## 验证

- 实施阶段 lint、build、格式检查通过；完整测试165项，163通过，2失败（累计名单与图标覆盖）。
- 规格审查独立复跑本批测试11项，9通过，2项上述已知失败。
- 规范审查独立复跑相关测试18项，16通过，2项上述已知失败。
- 浏览器初轮及修复后的针对性检查各覆盖390/768/1440、中英文、实际浅深主题12组合；图片缺项如实记录，不算图标通过。

结论：规范轴剩余1项判断建议；规格轴剩余3类阻塞，等待用户决定后继续实现、复验、双轴审查和提交闭环。

## 用户授权后的最终规格报告（原文）

**规格轴：无发现。**

按第07票验收标准及任务 `prd.md:78–86`，核对了完整25款、累计108项、39个平台快照和17个自动源，未发现缺项、错误实现或越界。

- `catalog.json:1229–1596`：平台、内核及身份符合独立期望；九项文字占位、两款历史记录和 Hey 源码入口未扩大例外范围。
- `model.ts:72–91`、`index.tsx:389`：历史记录禁止下载、fallback及版本日期，详情不生成下载按钮。
- 24条自动快照的时间与全部直链均对应实际 HTTP 核验记录，未用离线 fixture 冒充成功；OpenWrt独立版本与动态英文限定一致。

独立重跑定向测试 **23/23通过**；另核阅完整 **166/166**、构建及12组合浏览器证据。本轮未修改文件、运行Git或提交。

## 用户授权后的最终规范报告（原文）

**硬违规：无发现。** 已审查第4批全部25款及模型、同步调用方、详情条件渲染和中英动态文案。历史例外满足人工维护、空下载、无回退及版本时间约束；普通快照仍强制下载与回退入口。可选 `fallback` 未造成现有同步路径空值访问，三项授权未扩大适用范围。

**判断项（非阻塞）：** `tests/client-batch-4-sources.test.mjs:59–98,191` 当前验证跨平台合并资产集合及各平台数量；仍可增强为逐平台固定文件名与架构集合，捕获跨平台等量互换。此建议尚未实现，不记作已修复；其余 smell 无发现。

**验证：** 本轮定向测试23/23、只读类型检查及定向 lint 通过；已核对全套166/166与浏览器12组合记录，未声称本轮重跑。未改文件、运行 Git、提交或派生代理；按要求跳过格式检查。

两轴报告已在主会话原文转述。阻塞已随用户授权和对应实现解除；非阻塞判断建议如实保留，不假称完成。此前两轮阻塞报告为历史记录。
