# 代理客户端应用目录

## 状态

用户已于 2026-09-16 确认完整规格。需求与验收范围已定稿，尚未编写功能代码。

## 目标

参照 huarun.win 新增分类卡片与精简详情，基础资料人工维护，版本和官方下载入口自动更新。

## 已确认需求

- 在当前项目新增代理客户端目录，参照 https://huarun.win/ 的应用卡片样式与侧边栏平台分类。
- 收录范围按用户“完全直接抄应用部分”的要求理解为参考站全部应用；2026-09-16 读取参考站时显示 154 项，数量是当时快照，不是长期固定值。
- 平台分类包括全部应用、Android、iPhone / iPad、HarmonyOS、Windows、Mac、Linux、OpenWrt、华硕 / 梅林、Steam Deck。
- 保留搜索及平台、代理内核、代码状态、价格筛选。代理内核是客户端属性；不建设介绍内核发展历史的独立页面。
- 详情范围以用户截图为准：返回入口与面包屑、图标、名称、别名、简介、官方下载入口、适用平台、代码状态、完整功能费用、代理内核、主导开发、版本号、最后更新时间、数据来源。
- 详情在版本号、最后更新时间、数据来源这一行结束。不增加官方变更日志、当前版本官方预览或其后的其他区块。
- 不建设动态、人物、内核、协议、纪事独立栏目。
- 名称、简介、分类等基础资料人工维护；版本与官方下载链接自动更新。
- 详情页允许选择平台和安装包类型，再通过官方直链下载；只有商店入口的应用跳转官方应用商店。
- 本站不托管、不镜像安装包。
- 自动更新每 6 小时检查一次，并支持手动触发；仅收录正式版，不收录 Beta、Nightly、预发布或草稿版本。
- 官方仅有下载页面时，保留应用，按钮显示“前往官方下载页”。
- 无法自动获取版本信息时允许人工维护；未知信息留空，不推测版本号或发布时间。
- 某次更新失败时保留上次成功的数据，并显示最后成功核验时间；已确认失效的安装包直链改为官方获取入口。
- 列表提供默认顺序、最近更新两种排序，不提供热度排序与配套统计。

## 行为约定

- 在现有站点导航中新增“代理客户端”入口；目录内部使用参考站的平台侧栏和卡片布局，适配移动端，并沿用现有站点主题与语言机制。
- 搜索覆盖应用名称、别名和简介；不同筛选维度组合生效。多平台或多内核应用可出现在各自匹配的分类中。
- 保留筛选状态进入详情并返回列表，详情可直接打开；无匹配结果时可清除筛选。
- 默认顺序由目录资料维护；最近更新按官方版本发布时间降序排列，缺少发布时间的应用排在末尾，不使用核验时间冒充发布时间。
- 同一应用的不同平台允许拥有不同版本和发布时间；详情展示所选平台对应的版本、来源、安装包与核验时间。
- 安装包选项注明实际平台、架构与格式，仅提供有官方依据的组合；选择某版本时不可搭配另一版本的安装包。
- 人工维护的基础资料与自动更新的发布信息分开管理；支持人工维护无法自动获取的发布信息，自动获取失败不能清空已有值。
- 从应用官方发布渠道获取动态信息，参考站用于初始收录和展示参考，不作为实时下载中转。
- 单个应用或平台更新失败不阻止其他应用或平台更新；失败检查不得刷新最后成功核验时间。
- 超时、限流或临时访问受限本身不等于确认下载链接失效；确认失效才切换到官方获取入口。
- 主导开发与内核属性不跳转到本站未建设的人物、内核历史页面。

## 验收标准

- [ ] 新增入口可访问目录；首次收录按实际采集的参考站应用清单逐项核对，不以少量演示数据代替完整目录。
- [ ] 卡片与平台侧栏符合参考站布局，桌面及移动端可正常浏览。
- [ ] 搜索与平台、内核、代码状态、价格筛选可组合使用；多平台、多内核和无结果场景正确。
- [ ] 默认顺序稳定；最近更新按官方发布时间排序，未知日期在末尾。
- [ ] 卡片进入对应详情，直接访问详情有效，返回列表保留筛选；详情在截图规定的版本信息行结束。
- [ ] 选择平台和安装包时，显示正确的版本、架构、格式与官方下载地址；商店及官方下载页按钮明确区分。
- [ ] 定时检查间隔配置为 6 小时，同时可手动触发；仅获取正式版。
- [ ] 自动更新不覆盖人工维护的名称、简介与分类；无法自动获取的版本可人工维护，未知值不虚构。
- [ ] 覆盖正常更新、无正式版、部分来源失败的同步检查；失败保留旧数据和最后成功核验时间，其他应用继续更新。
- [ ] 已确认失效的直链回退官方获取入口；临时访问失败不误删直链。
- [ ] 不托管或代理传输安装包；不生成被排除的独立栏目和详情区块，不留下不存在的站内人物或内核页面链接。
- [ ] 完成与目录筛选、下载匹配和同步逻辑相称的检查，以及项目要求的构建与测试。

## 规格确认

Q1–Q6 均已由用户明确答复，没有遗留访谈问题。用户在完整规格确认请求后回复“没问题”，确认本文需求、行为约定与验收标准。

### 已确认例外：FoXray（2026-09-16）

第 04 票核验发现 FoXray 官方仓库返回 404、美国区 Apple lookup 无结果、原官网不可用。用户在获知审查阻塞后明确回复“接受例外”：保留该应用身份及当前无法获取说明、文字占位图、未知版本和日期、历史官方商店入口，不要求当前可下载或取得官方图标。不填写自动核验时间，不用第三方包或测试版替代。此例外仅适用于 FoXray，不豁免其他应用的官方资料与可用入口要求。

### 已确认例外：第 05 票图标与 Clash MIX 获取入口（2026-09-16）

用户明确回复“接受两项例外”：InsightBox、Box for Android、akashaProxy 可使用明确的文字占位图，不把共享频道头像或第三方图片冒作官方图标。Clash MIX 可使用文字占位图，仅链接官方源码仓库，并明确“当前未确认可安装的官方正式包”；正式版本、发布时间和成功核验时间留空，不提供失效发布入口或第三方包。此授权只覆盖上述四项的图标，以及 Clash MIX 的源码获取方式；不豁免其他属性、平台、正式性及入口核验，也不扩展至其他应用。

### 已确认例外：Box4Magisk 图标（2026-09-16）

用户在获知 Box4Magisk 官方仓库中的图片属于 Vite 默认素材、尚无可核实应用图标后回复“允许”，同意该应用使用明确的文字占位图。此授权仅覆盖 Box4Magisk 图标，不改变其官方发布、平台、架构和资料核验要求。

### 已确认例外：第 06 票 Pantheon 与 ClashTui（2026-09-16）

用户在获知两项阻塞后明确回复“接受两项例外”。Pantheon 保留历史应用身份，使用明确的文字占位图，并说明“当前无法获取”；不要求当前可用的官方获取入口，版本和发布时间留空，不填写成功核验时间，不用第三方包或未经核实资料替代。ClashTui 仅允许使用明确的文字占位图，其官方发布入口、平台、架构和其他资料仍须正常核验。此授权仅适用于这两项，不扩展至其他应用。

### 已确认例外：第 07 票历史应用、图标与 Hey 源码获取（2026-09-16）

用户在获知三类阻塞及处理方案后明确回复“接受三项例外”：

- Clash .NET、lvory 保留冻结清单中的历史身份及历史 Windows 分类，说明“当前无法获取”，使用明确的文字占位图；不要求可用的官方获取入口。不提供未经核实或第三方下载链接，未核实的内核、开发者、版本、发布时间与成功核验时间留空，代码状态及费用标为待核实；历史收录来源不冒作官方下载来源。
- ShellCrash、PassWall、PassWall2、Nikki、Nikki RS、Momo、HomeProxy 允许使用明确的文字占位图。该例外仅涉及图标，其余资料、平台、来源与获取要求不变。
- Hey 允许仅链接官方源码仓库，并明确需要自行构建和签名，不宣称提供预编译安装包；正式版本、发布时间与成功核验时间留空。

上述授权仅适用于列出的应用及事项，不扩展至其他应用。

### 已确认例外：第 08 票 ShadowSocksR Plus+ 图标（2026-09-17）

用户在获知官方仓库未找到可核实应用图标、现有例外不覆盖该应用后，明确回复“授权文字占位图”。ShadowSocksR Plus+ 允许使用明确标注非官方的文字占位图。此授权仅覆盖该应用图标，不改变其他资料、平台、正式发布、架构及官方获取入口的核验要求，也不扩展至其他应用。

## 项目现状

- 项目已有顶级导航与静态目录数据模式，可新增独立入口承载应用目录。
- `.github/workflows/sync-upstream.yml` 已有定时执行机制，但其用途是合并上游代码。
- 当前没有客户端版本与下载链接更新器，也没有 Worker 定时处理函数。自动同步需要新增实现。

## 参考依据

- 参考站应用列表：https://huarun.win/ ，读取日期：2026-09-16。
- 多平台详情参考：https://huarun.win/apps/flclash 。
- 截图对应详情：https://huarun.win/apps/shadowrocket 。
- 用户截图：截屏 2026-09-16 at 15.26.15.png。
- 首轮用户答复：自动更新版本与链接；人工维护基础资料；保留内核属性及筛选；按平台和安装包选择下载，商店应用跳商店。
- 第二轮用户答复：每 6 小时检查正式版并支持手动触发；无直链跳官方下载页，无法自动获取的版本可人工维护；失败保留旧数据和成功核验时间，确认失效则回退官方入口；默认顺序与最近更新两种排序。

## 验收追加：分页与组件统一（第 11 票，已确认）

用户已确认仅修改代理客户端列表与详情；本次 `/atw-implement` 授权实施已确认的第 11 票，不代表父任务最终验收。

- 固定每页 24 项，先筛选、排序再分页；展示匹配总数、当前范围、页码、上一页与下一页。当前 154 项应为 7 页，末页 10 项，无重复或遗漏。不增加条数选择器或无限滚动。
- 页码与筛选共同保存于 URL；刷新、详情返回、浏览器前进后退保留状态。搜索、平台分类、其他筛选或排序改变回第一页；清除筛选同时重置页码，保留语言及无关参数。
- 非法页码回第一页，超过末页归到有效末页；无结果保留空态和清除筛选，不出现无效分页。
- 六处选择控件均使用 shadcn/ui Select，触发器与展开选项均符合主题；分页采用 Pagination，平台按钮、卡片、标签、表单标签、面包屑等有对应组件的部分统一复用 shadcn/ui。普通文本、图片、语义链接及布局不机械包装。
- 不改变全站其他页面或共享组件既有默认行为，不以宽泛 CSS 覆盖组件；不改人工目录、发布快照、同步与部署流程。保留平台／版本／包隔离、官方入口、历史不可获取例外和精简详情结束位置。
- 验收覆盖分页及 URL 边界回归、原目录与同步测试；实际浏览器检查 390／768／1440 宽度、中英文、实际深浅主题、选择器展开／选择／关闭、长包名、分页、焦点与键盘，并查看截图。先构建再完整测试、lint、改动文件格式检查，完成规格与规范两轴审查。

## 验收追加：内核筛选归并（第 12 票，已确认）

用户已确认第 12 票中的归并方案，本次 `/atw-implement` 授权实施，不代表父任务最终验收。

- 内核筛选固定为九项，顺序为：全部、Mihomo、sing-box、自研、Xray、V2Ray、meow-rs、clash-rs、其他。继续使用现有 shadcn/ui Select，不改为胶囊按钮。
- 筛选分类与具体内核资料分离；卡片和详情保留原有准确名称，不修改人工目录或发布快照。逐项记录当前 29 个内核字符串的分类及必要依据；别名需核对官方证据，不凭名称相似归并。
- 自研须有明确依据，闭源或未知不等于自研；无法明确归类及空内核放入其他，详情的未知仍保持未知。多内核应用可命中多个对应分类。
- 保持搜索、平台、价格、代码状态的组合行为；筛选变化回第一页，保留详情返回、语言和无关 URL 参数。核验旧内核参数，避免静默扩大匹配，不增加无关兼容层。
- 独立期望测试覆盖九项顺序、主流分类、核实别名、自研、其他、未知、多内核、组合筛选、页码重置及空态。实际浏览器验证移动和桌面、中英文、真实深浅主题、选择器展开／选择／键盘、代表应用详情保留、URL 和前进后退。
- 不修改自动同步、下载匹配、部署、其他页面或历史例外。先构建再完整测试，lint 和改动文件格式检查通过，完成规格／规范两轴审查。
